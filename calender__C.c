#include<stdio.h>
struct bank{
int acc_no;
char name[100];
int balance;}s[100];
int main()
{
    int i;

    for(i=0;i<10;i++)
    {
        printf("enter account number");
        scanf("%d",&s.acc_no[i]);
        printf("enter name");
        scanf("%s",&s.name[i]);
        printf("enter balance");
        scanf("%d",&s.balance[i]);
    }
    if()



}
